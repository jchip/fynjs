# npm/fyn settings for this monorepo.
#
# No registry is set here on purpose: npm and fyn then use whatever registry you have
# configured (your user ~/.npmrc, a company mirror, a proxy), which is almost always what
# you want. Pin one here only if this repo must ignore that and always use a specific
# registry.
#
# registry=https://registry.npmjs.org/
